== Omniauth on heroku

This is a demo app for omniauth on heroku. Since heroku doesn't support filesystem storage, here I am demoing with the memcached storage.

Don't forget to replace your Keys/secrets in config/omniauth.rb
